import React, { Component } from 'react';

export default class MediaHeader extends Component {
  render() {
    return (
        <div className="media-header">
            <a id='addPathBtn' className="btn btn-link">Add</a>
        </div>
    );
  }
}
